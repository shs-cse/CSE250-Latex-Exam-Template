\documentclass[a4paper, addpoints]{exam}
\usepackage{anyfontsize, amssymb}
\usepackage[shortlabels]{enumitem}
\usepackage[siunitx, american, RPvoltages]{circuitikz}
\sisetup{parse-numbers=false}

% short-hand for resistor units, e.g. 
% in circuitikz: 1<\MO>, 2<\mA>, 3<\kV>, 4<\uF>, 5<\nH>, 6<\C> 
% in math-mode: \qty{1}{\MO}
\DeclareSIUnit{\mO}{\milli\ohm}
\def\O{\ohm}
\DeclareSIUnit{\O}{\ohm}
\DeclareSIUnit{\kO}{\kilo\ohm}
\DeclareSIUnit{\MO}{\mega\ohm}


% title variables
\def\semester{Spring 2023}
\def\coursecode{CSE250}
\def\coursename{Circuits And Electronics}
\def\sect{05}
\def\faculty{SHS}
\def\assessment{Quiz 1}
\def\setnum{A}
\def\duration{30 minutes}
\advance\day by 0
\input{header.tex}



% marks instead of points
\marksnotpoints
\def\marks#1{%
    \ifthenelse{\equal{#1}{1} \or \equal{#1}{\half}}
    {#1 mark}{#1 marks}%
}%
% use first brackets for \subpart
\renewcommand{\subpartlabel}{(\thesubpart)}
% formatting of Question blocks
\qformat{
    \parbox[][24pt][t]{0.95\textwidth}{ % for creating space below
        \Large\textbf{
            $\blacksquare$
            Question \thequestion\ of \numquestions\;%
            \textit{%
                \if\thequestiontitle\thequestion%
                \else[\thequestiontitle]\;%
                \fi%
                [\marks\totalpoints%
                    \if\totalbonuspoints 0%
                    \else \,+\,\marks\totalbonuspoints\;(bonus)%
                    \fi%
                ]
            }
        }
    }
    \hfill
}
% formatting of Bonus Question blocks
\bonusqformat{
    \parbox[][24pt][t]{0.95\textwidth}{ % for creating space below
        \Large\textbf{
            $\blacksquare$
            Question \thequestion\ of \numquestions\;%
            \textit{%
                \if\thequestiontitle\thequestion%
                \else[\thequestiontitle]\;%
                \fi%
                [\marks\totalbonuspoints\;(bonus)]
            }
        }
    }
    \hfill
}
% formatting of subpart, subsubpart etc.
\pointformat{\small\textbf{\textit{[\thepoints]}}}
% formatting of bonussubpart, bonussubsubpart etc.
\bonuspointformat{\small\textbf{\textit{[\thepoints]}}}




\begin{document}
\begin{center}
    \fbox{%
        \fbox{%
            \parbox{0.95\textwidth}{%
                \begin{itemize}[\checkmark,nosep,leftmargin=16pt]
                    \item No washroom breaks. Phones must be turned off. Using/carrying any notes during the exam is not allowed.
                    \item At the end of the exam, both the \textbf{answer script} and the \textbf{question paper} must be returned to invigilator.
                    \item All \textbf{\numquestions\ questions} are compulsory. Marks allotted for each question are mentioned beside each question.
                    \item Bonus questions are indicated as ``\textbf{\textit{(bonus)}}" along with allotted marks.
                    \item Write your answers inside the indicated boxes. In case you run out of room for an answer, please continue on the back of the page.
                \end{itemize}
            }%
        }%
    }
\end{center}


\begin{questions}
    \foreach \quesno in {1,...,99}{
            \InputIfFileExists{questions/\quesno.tex}{}{
                \InputIfFileExists{questions/0\quesno.tex}{}{}
            }
            \filbreak
        }
\end{questions}
\end{document}